name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          node-version: 16
      - name: Run build script
        run: |
          cd frontEnd
          npm run build
