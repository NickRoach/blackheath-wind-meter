name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Run build script
        run: |
          cd frontEnd
          npm ci
          npm run build
